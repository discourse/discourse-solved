be:
  js:
    solved:
      topic_status_filter:
        all: "усе"
    discourse_automation:
      triggerables:
        first_accepted_solution:
          fields:
            maximum_trust_level:
              label: узровень даверу
