{{#if siteSettings.solved_enabled}}
  <ComboBox
    @class="solved-status-filter"
    @content={{statuses}}
    @value={{status}}
    @valueProperty="value"
    @options={{hash caretDownIcon="caret-right" caretUpIcon="caret-down"}}
    {{on "change" (action "changeStatus")}}
  />
{{/if}}
